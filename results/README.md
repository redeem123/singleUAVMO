# Results Workspace

`results/` is the default output directory for benchmark runs and reports.

- Runtime contents here are generated and intentionally ignored in Git.
- Keep this directory in place so existing CLI defaults and scripts continue to work.
- Commit only this placeholder README, not run artifacts.
