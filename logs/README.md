# Logs Workspace

`logs/` stores generated run logs, PID files, and other transient execution metadata.

- Contents are ignored in Git.
- Keep this directory in place for stable script and operator workflows.
- Commit only this placeholder README, not generated logs.
