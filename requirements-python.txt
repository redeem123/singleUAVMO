numpy>=1.24
scipy>=1.10
matplotlib>=3.7
pyyaml>=6.0
numba>=0.58.0
# Torch is required for checkpointed SAC-SMOPSO training/evaluation.
# Keep this aligned with a current PyTorch release that ships Python 3.14 wheels.
torch>=2.10
