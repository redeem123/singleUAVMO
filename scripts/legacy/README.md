# Legacy Scripts

This directory holds preserved historical utilities that are not part of the active package surface.

- `problem_generation/`: old standalone terrain-generation scripts moved out of the repo root.
- `test_eval.py`: scratch evaluation helper retained for traceability.

Prefer the active package and CLI entrypoints for new work. Keep new legacy-only helpers here rather than at the repo root.
